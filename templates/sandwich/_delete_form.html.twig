<form method="post" action="{{ path('sandwich_delete', {'id': sandwich.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer ce sandwich ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sandwich.id) }}">
    <button class="btn btn-danger" id="btnSuppr">Supprimer</button>
</form>
